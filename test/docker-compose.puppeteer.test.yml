version: '2'

services:
  sut:
    build:
      context: .
      dockerfile: Dockerfile.puppeteer
    depends_on:
      - ispconfig-test
    networks:
      - test_shared
    cap_add:
      - SYS_ADMIN
    environment:
      - PORT=443
    volumes:
      - './default:/app/test:ro'

  ispconfig-test:
    build:
      args:
        BUILD_ISPCONFIG_PORT: "443"
    extends:
      file: ../docker-compose.template.yml
      service: ispconfig
    networks:
      - test_shared

networks:
  test_shared:
    external: true
