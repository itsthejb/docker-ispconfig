#!/bin/bash -xe

trap "echo Exited!; exit;" SIGINT SIGTERM

for YML in $(ls *.test.yml); do
  if ! docker-compose -f $YML up --build --remove-orphans sut; then
    echo "Build for $YML failed!"
    exit 1
  fi
done
